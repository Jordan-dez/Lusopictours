{% extends 'base.html.twig' %}

{% block title %}Inscription|LusoPictours{% endblock %}

{% block content %}
<div class="container mt-5">
    <h1>Inscription</h1>
        {% if notification %}
        <div class="alert alert-info">{{notification}}</div>
        {% endif %}
        {{form(formulaire)}}
</div>
{% endblock %}
