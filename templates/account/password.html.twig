{% extends 'base.html.twig' %}
{% block content %}
    <div class="container mt-5">
        <h1>
            Modifier mon mot de passe
            
            
            		
        </h1>
        <a href="{{ path('app_account') }}">
            retourneer à mon compte
            			
            <a>
                {% if notification %}
                    <div class="alert alert-info">
                        {{ notification }}
                    </div>
                {% endif %}
                {{ form(formulaire) }}
                </div>
            {% endblock %}
            